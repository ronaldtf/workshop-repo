version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: <TASK_DEFINITION>
        LoadBalancerInfo:
          ContainerName: "webapp"
          ContainerPort: 80git add appspec.yaml
git commit -m "Adding AppSpec"
git push -u origin main